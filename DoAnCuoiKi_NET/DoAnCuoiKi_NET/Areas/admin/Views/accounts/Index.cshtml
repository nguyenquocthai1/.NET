@model IEnumerable<DoAnCuoiKi_NET.Models.Account>
@{
    ViewBag.Title = "Quản lý Tài Khoản";
    Layout = "~/Areas/admin/Views/Shared/_MyLayout.cshtml";
}
@section _Header
{
    <!-- Custom styles for this page -->
    <link href="~/Areas/admin/Assets/vendor/datatables/dataTables.bootstrap4.min.css" rel="stylesheet">
}
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h4 class="m-0 font-weight-bold text-center" style="color: #1a53bd">Quản lý Tài khoản</h4>
    </div>
    <div class="card-body">
        <!-- /.panel-heading -->
        <div class="table-responsive">
            <p>
                <a href="@Url.Action("Create", "accounts")" class="btn btn-success btn-icon-split">
                    <span class="icon text-white-50">
                        <i class="fa fa-magic"></i>
                    </span>
                    <span class="text">Thêm mới</span>
                </a>
            </p>
            <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>
                            STT
                        </th>
                        <th>
                            Tên tài khoản
                        </th>

                        <th>
                            Mật khẩu
                        </th>

                        <th>
                            Tên người dùng
                        </th>

                        <th>
                            Số điện thoại
                        </th>

                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @{var Number = 1; }
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.Raw(Number++)
                            </td>
                            <td style="max-width: 80px; vertical-align: middle">
                                @Html.DisplayFor(modelItem => item.username)
                            </td>

                            <td style="max-width: 80px; vertical-align: middle;">
                                @Html.DisplayFor(modelItem => item.password)
                            </td>
                            <td style="max-width: 80px; vertical-align: middle">
                                @Html.DisplayFor(modelItem => item.firstname)
                                @Html.DisplayFor(modelItem => item.lastname)
                            </td>

                            <td style="max-width: 80px; vertical-align: middle">
                                @Html.DisplayFor(modelItem => item.phone)
                            </td>

                            <td>
                                <a href="@Url.Action("Edit", "accounts", new { id = item.id_account })" class="btn btn-warning btn-icon-split">
                                    <span class="icon text-white-50">
                                        <i class="fas fa-edit"></i>
                                    </span>
                                    <span class="text">Sửa</span>
                                </a>

                                <a href="@Url.Action("Details", "accounts", new { id = item.id_account })" class="btn btn-primary btn-icon-split">
                                    <span class="icon text-white-50">
                                        <i class="fas fa-info-circle"></i>
                                    </span>
                                    <span class="text">Chi tiết</span>
                                </a>

                                <a href="@Url.Action("Delete", "accounts", new { id = item.id_account })" class="btn btn-danger btn-icon-split">
                                    <span class="icon text-white-50">
                                        <i class="fas fa-trash"></i>
                                    </span>
                                    <span class="text">Xóa</span>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <!-- /.panel-body -->
    </div>
    <!-- /.panel -->
</div>
<!-- /.col-lg-12 -->
</div>
<!-- /.row -->
@section _Footer
{
    <!-- Page level plugins -->
    <script src="~/Areas/admin/Assets/vendor/datatables/jquery.dataTables.min.js"></script>
    <script src="~/Areas/admin/Assets/vendor/datatables/dataTables.bootstrap4.min.js"></script>
    <!-- Page level custom scripts -->
    <script src="~/Areas/admin/Assets/js/demo/datatables-demo.js"></script>

    <style>
        /* Set max width for table cells */
        td, th {
            overflow: auto;
            white-space: nowrap;
            text-align: center;
        }
    </style>
}
